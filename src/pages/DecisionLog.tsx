import React, { useState } from 'react'; const DecisionLog: React.FC = () => { const [decisions, setDecisions] = useState([{ date: '2023-10-01', decision: 'Increase production by 10%', rationale: 'Expected demand surge in Q4' }]); return (<div className="p-6"><h1 className="text-2xl font-bold mb-4">Decision Log</h1><div className="overflow-x-auto"><table className="min-w-full bg-white"><thead><tr><th className="py-2 px-4 border-b">Date</th><th className="py-2 px-4 border-b">Decision</th><th className="py-2 px-4 border-b">Rationale</th></tr></thead><tbody>{decisions.map((entry, index) => (<tr key={index}><td className="py-2 px-4 border-b">{entry.date}</td><td className="py-2 px-4 border-b">{entry.decision}</td><td className="py-2 px-4 border-b">{entry.rationale}</td></tr>))}</tbody></table></div></div>); }; export default DecisionLog;